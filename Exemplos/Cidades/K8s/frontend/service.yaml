apiVersion: v1
kind: Service
metadata:
  name: cidades-frontend
  labels:
    run: cidades-frontend
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: cidades-frontend
