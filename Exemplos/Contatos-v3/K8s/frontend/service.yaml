apiVersion: v1
kind: Service
metadata:
  name: contatos-frontend
  labels:
    run: contatos-frontend
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: contatos-frontend
