apiVersion: v1
kind: Service
metadata:
  name: contatos-backend
  labels:
    run: contatos-backend
spec:
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
  selector:
    app: contatos-backend
