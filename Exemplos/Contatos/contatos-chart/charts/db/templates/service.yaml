apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-{{ .Values.global.db.name }}
  labels:
    run: {{ .Release.Name }}-{{ .Values.global.db.name }}
spec:
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
  selector:
    app: {{ .Release.Name }}-{{ .Values.global.db.name }}
