apiVersion: v1
kind: Service
metadata:
  name: contatos-backend
  labels:
    run: contatos-backend
spec:
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
  selector:
    app: contatos-backend
